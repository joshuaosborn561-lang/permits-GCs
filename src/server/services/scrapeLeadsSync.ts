/**
 * Legacy module name kept for imports.
 * Permit & Parcel MCP uses syncToSupabase.ts (Maps-style ingest RPCs).
 */
export { syncToSupabase, syncParcelsToSupabase, syncContractorsToSupabase } from './syncToSupabase.js';
